# Download and make executable
chmod +x httpverb.sh

# Or install system-wide
sudo cp httpverb.sh /usr/local/bin/httpverb
sudo chmod +x /usr/local/bin/httpverb

# Then use it from anywhere
httpverb -d example.com
